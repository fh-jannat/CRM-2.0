'use client';
import StudentLayoutRole from './Studentlayout';

const StudentLayout = ({ children }: any) => {
  return <StudentLayoutRole>{children}</StudentLayoutRole>;
};

export default StudentLayout;
