import PotentialRoleLayout from './PotentialRoleLayout';

export default function Layout({ children }: { children: React.ReactNode }) {
  return <PotentialRoleLayout>{children}</PotentialRoleLayout>;
}
