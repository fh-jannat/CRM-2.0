'use client';
import React from 'react';
import { LeadDetails } from '@/components/leads/lead-details/LeadDetails';

function ProfileWrapper() {
  return <LeadDetails />;
}

export default ProfileWrapper;
